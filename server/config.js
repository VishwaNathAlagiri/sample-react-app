module.exports = {
  secret: "Belong to VISHWA",
  baseUrl: "http://localhost:8081/",
  services: {
    OnTrack: {
      target: "OnTrack",
    },
    onDelayed: {
      target: "onDelayed",
    },
    onHold: {
      target: "onHold",
    }
  }
};
